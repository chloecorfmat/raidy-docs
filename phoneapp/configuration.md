# Utilisation de l'API

Les applications mobiles utilisent l'API pour faire des requêtes vers la base de données.

L'URL de l'API doit être renseignée dans la variable `api_path` du fichier `www/js/variables.js`